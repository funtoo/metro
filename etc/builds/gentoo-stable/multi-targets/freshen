# This is the stage3-freshen target, which does an in-place upgrade of a stage3
# (using a deep "emerge" update) rather than re-generating the whole thing.
# Notice that we also build extras (virtual templates).  Remove "$[:extras]" if
# this is not desired.

[section multi]

targets: $[:snapshot] stage3-freshen $[:extras]

