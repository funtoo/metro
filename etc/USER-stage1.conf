[section target]

: stage1
name: $[]-$[target/subarch]-$[target/version]
version: 2008.10.10
subarch: amd64

[section portage]

name: portage-$[target/version]
USE: $[portage/HOSTUSE]

[section source]

: stage3
name: $[]-$[source/subarch]-$[source/version]
version: << $[control]/lastdate
subarch: << $[control]/subarch

[section emerge]

packages:

