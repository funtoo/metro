# This file defines values common to intermediate stages -- stage1 and stage2.
# These stages are called "intermediate" because they are considered to be
# intermediate binary-images generated in the process of creating a stage3.

[section target]

name: $[target]-$[target/subarch]-$[target/version]
name/current: $[target]-$[target/subarch]-current
